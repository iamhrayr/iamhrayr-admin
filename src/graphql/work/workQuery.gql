query work($id: ID!) {
  work(id: $id) {
    id
    title
    description
    thumbnail {
      url
      key
    }
    images {
      url
      key
    }
    tags
    category {
      id
      name
    }
  }
}
